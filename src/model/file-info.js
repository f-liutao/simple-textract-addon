export class FileInfo {
    constructor(fileName, hash, lastUpdated, text) {
        this.fileName = fileName;
        this.hash = hash;
        this.lastUpdated = lastUpdated;
        this.text = text;
    }
}
